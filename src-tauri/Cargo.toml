[package]
name = "claude-dashboard-launcher"
version = "1.0.0"
edition = "2021"
description = "Lightweight launcher for Claude Usage Dashboard"
authors = ["JerrettDavis"]
license = "MIT"

[dependencies]

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "z"
strip = true
